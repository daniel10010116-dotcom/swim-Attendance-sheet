{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/catch-all?path=$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
